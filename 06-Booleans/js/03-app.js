const autenticado = true;

if (autenticado) {
    console.log('Si, podes ver Netflix');
} else {
    console.log('No, no podes ver Netflix');
}

// Operador ternario
console.log(autenticado ? 'Si esta autenticado' : 'No esta autenticado');