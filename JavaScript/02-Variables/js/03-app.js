const producto = 'Tablet';

// Las variables no se pueden reasignar.

// producto = 'Monitor';

console.log(producto);

// Si o si se tienen que inicializar las variables.
const precio = 20;
console.log(precio);